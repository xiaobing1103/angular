import { LcUppercasePipe } from './lc-uppercase.pipe';

describe('LcUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new LcUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
